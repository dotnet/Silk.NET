<Project>
    <PropertyGroup>
        <SilkDescription>This package contains bindings for the low-level, high-throughput Vulkan graphics and compute API.</SilkDescription>
    </PropertyGroup>
</Project>
